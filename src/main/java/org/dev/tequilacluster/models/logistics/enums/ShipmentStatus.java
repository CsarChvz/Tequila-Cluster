package org.dev.tequilacluster.models.logistics.enums;

/** Valores permitidos para shipment.status (CHECK de la tabla shipment). */
public enum ShipmentStatus {
    PLANNED,
    IN_TRANSIT,
    DELIVERED,
    CANCELLED
}
