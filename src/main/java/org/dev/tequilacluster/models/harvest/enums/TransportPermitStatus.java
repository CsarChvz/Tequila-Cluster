package org.dev.tequilacluster.models.harvest.enums;

/** Valores permitidos para transport_permit.status (CHECK de la tabla transport_permit). */
public enum TransportPermitStatus {
    GENERATED,
    AUTHORIZED,
    IN_TRANSIT,
    COMPLETED,
    CANCELLED
}
