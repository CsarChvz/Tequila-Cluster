package org.dev.tequilacluster.models.shared.enums;

/** Valores permitidos para process_alert.severity (CHECK de la tabla process_alert). */
public enum AlertSeverity {
    INFO,
    WARNING,
    CRITICAL
}
