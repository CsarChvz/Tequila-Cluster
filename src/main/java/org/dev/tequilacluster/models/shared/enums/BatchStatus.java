package org.dev.tequilacluster.models.shared.enums;

/** Valores permitidos para batch.status (CHECK de la tabla batch). */
public enum BatchStatus {
    DRAFT,
    IN_PROGRESS,
    COMPLETED,
    CANCELLED
}
