package org.dev.tequilacluster.utils.security;

/** The four permission flags on {@code role_stage_permission} (FR-03, RB-504). */
public enum StageAction {
    VIEW,
    CREATE,
    UPDATE,
    COMPLETE
}
